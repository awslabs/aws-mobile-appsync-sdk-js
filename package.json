{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "integration-test": "cd integration-tests && npm install && npm test",
    "validate": "./scripts/validate-build.sh",
    "preversion": "npm run build && npm run test && npm run integration-test && npm run validate"
  },
  "workspaces": [
    "packages/*"
  ],
  "resolutions": {
    "graphql-tag": "^2.12.6",
    "@types/babel__traverse": "7.0.6"
  },
  "devDependencies": {
    "@swc/core": "^1.13.19",
    "@types/graphql": "0.12.4",
    "@types/jest": "^29.5.12",
    "@types/node": "^8.0.46",
    "@types/zen-observable": "^0.5.3",
    "graphql": "^16.0.0",
    "graphql-tag": "^2.12.6",
    "jest": "^29.7.0",
    "lerna": "^2.9.0",
    "uuid": "^10",
    "node-fetch": "^2.2.0",
    "ts-jest": "^29.2.5",
    "tsup": "^8.5.0",
    "typescript": "~4.9.5",
    "zen-observable-ts": "^1.1.0"
  }
}
